import React from 'react';

import './index.css';

export interface CanvasProps {};

export function Canvas(props: CanvasProps) {
    return (
        <svg
            id = 'Canvas'
        >

        </svg>
    );
};